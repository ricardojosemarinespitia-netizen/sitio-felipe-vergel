
# Material fuente pesado y documentos personales
LAMPARAS-*.zip
*.zip
RUT*.pdf
FOTO.jpg

# Carpeta de trabajo: copia de TODAS las fotos ordenadas por producto y
# renombradas con el color de la pieza (para revisar con Felipe). Son
# duplicados de img/ (21 MB), no deben publicarse ni pesar en el repo.
FOTOS POR COLOR/

# Carpetas de correcciones puntuales (zips de fotos que Felipe manda por
# Drive para reemplazar/agregar colores). Una vez copiadas a img/ y
# publicadas, esta carpeta ya no hace falta en el repo.
CORRECCION*/

# Codigo de backend (Apps Script) — NUNCA se publica junto al sitio.
# GitHub Pages sirve como estatico todo lo que este en el repo: si un .gs
# vive aqui, cualquiera lo descarga con su URL y se lleva los SECRETO.
_backend-no-publicar/
*.gs

# ============================================================
#  fv-functions/ es OTRO repositorio de git (tiene su propio .git y su
#  propio remote: github.com/.../fv-functions.git). Se despliega solo,
#  a Netlify (exquisite-pasca-848b8b), NO con este sitio.
#
#  Sin esta linea, un "git add -A" rutinario aqui lo commitea como
#  gitlink de submodulo sin declarar: se sube una referencia rota que
#  nadie puede clonar y el codigo de las funciones no viaja.
#  Comprobado con: git add --dry-run -A
#    warning: adding embedded git repository: fv-functions
#
#  Para publicar las funciones: entrar a sitio-felipe-vergel/fv-functions
#  y hacer commit + push ALLI (rama main).
# ============================================================
fv-functions/
